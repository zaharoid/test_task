export const adverts = (state) => state.adverts.items;
export const isLoading = (state) => state.adverts.isLoading;
export const error = (state) => state.adverts.error;
export const favorites = (state) => state.favorites.items;
